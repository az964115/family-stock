name: Daily Asset Snapshot
on:
  schedule:
    - cron: '0 6 * * 1-5' # 每週一至週五台灣時間下午 14:00 執行
jobs:
  trigger_snapshot:
    runs-on: ubuntu-latest
    steps:
      - name: Trigger Firebase Backup
        run: |
          # 透過 GitHub 自動觸發，你可以把網頁佈署在 GitHub Pages 上，並在此處 curl 該網址加上特定參數 地址.github/workflows/daily_snapshot.yml
          curl -s "https://az964115.github.io/family-stock/?trigger=jason_snapshot"
